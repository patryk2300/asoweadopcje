import { DisplayImageDirective } from './display-image.directive';

describe('DisplayImageDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayImageDirective();
    expect(directive).toBeTruthy();
  });
});
